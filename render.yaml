
services:
  - type: web
    name: express-server
    env: node
    buildCommand: yarn install && yarn build
    startCommand: node dist/server/start-server.js
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
