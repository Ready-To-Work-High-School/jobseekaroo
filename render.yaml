
services:
  - type: web
    name: express-server
    env: node
    buildCommand: yarn install && yarn build
    startCommand: node src/server/start-server.cjs
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
