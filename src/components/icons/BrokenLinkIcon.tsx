
import React from 'react';
import { Link2Off } from 'lucide-react';

const BrokenLinkIcon = ({ size = 24, color = 'currentColor' }) => {
  return <Link2Off size={size} color={color} />;
};

export default BrokenLinkIcon;
