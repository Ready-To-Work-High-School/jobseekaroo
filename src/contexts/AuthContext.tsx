
// Re-export everything from the main auth context for backward compatibility
export * from './auth/AuthContext';
export { useAuth } from '@/hooks/useAuth';
export { AuthProvider } from './auth/AuthProvider';
